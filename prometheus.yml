global:
  scrape_interval: 15s

scrape_configs:
  # Scrape the metrics the OTel Collector re-exports from bff + api.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
